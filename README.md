<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>@LINEFUNERAL12 | YouTube Kanalı</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Inter', sans-serif;
      background: radial-gradient(1200px 600px at 10% -10%, #1f2937 0%, transparent 60%),
                  radial-gradient(800px 400px at 90% 10%, #111827 0%, transparent 55%),
                  #0b0f19;
      color: #e5e7eb;
      line-height: 1.6;
    }
    a { color: inherit; text-decoration: none; }
    .container { max-width: 1100px; margin: auto; padding: 32px; }

    header {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 16px;
      align-items: center;
      margin-bottom: 64px;
    }
    .avatar {
      width: 96px; height: 96px; border-radius: 50%;
      background: linear-gradient(135deg, #6b7280, #111827);
      display: flex; align-items: center; justify-content: center;
      font-weight: 800; font-size: 24px;
      box-shadow: 0 20px 40px rgba(0,0,0,.4);
    }
    .title h1 { font-size: 36px; font-weight: 800; }
    .title p { color: #9ca3af; margin-top: 6px; }
    .cta a {
      background: linear-gradient(135deg, #ef4444, #b91c1c);
      padding: 14px 22px;
      border-radius: 999px;
      font-weight: 600;
      box-shadow: 0 10px 25px rgba(239,68,68,.35);
    }

    .hero {
      display: grid;
      grid-template-columns: 1.2fr .8fr;
      gap: 40px;
      margin-bottom: 80px;
    }
    .hero h2 { font-size: 42px; font-weight: 800; margin-bottom: 16px; }
    .hero p { color: #d1d5db; }
    .stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 16px;
      margin-top: 28px;
    }
    .stat {
      background: rgba(255,255,255,.04);
      border: 1px solid rgba(255,255,255,.08);
      border-radius: 20px;
      padding: 20px;
      text-align: center;
      backdrop-filter: blur(6px);
    }
    .stat strong { font-size: 28px; display: block; }
    .card {
      background: linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.02));
      border: 1px solid rgba(255,255,255,.1);
      border-radius: 28px;
      padding: 28px;
      box-shadow: 0 20px 40px rgba(0,0,0,.35);
    }

    .videos {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 24px;
      margin-bottom: 80px;
    }
    .video {
      background: rgba(0,0,0,.35);
      border-radius: 22px;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,.08);
      transition: transform .25s ease, box-shadow .25s ease;
    }
    .video:hover { transform: translateY(-6px); box-shadow: 0 20px 40px rgba(0,0,0,.45); }
    .thumb {
      aspect-ratio: 16/9;
      background: linear-gradient(135deg, #111827, #030712);
      display: flex; align-items: center; justify-content: center;
      color: #6b7280; font-weight: 600;
    }
    .video .info { padding: 16px; }
    .video h4 { font-size: 16px; margin-bottom: 6px; }
    .video span { color: #9ca3af; font-size: 14px; }

    footer {
      text-align: center;
      color: #9ca3af;
      padding: 40px 0 10px;
      border-top: 1px solid rgba(255,255,255,.08);
    }

    @media (max-width: 900px) {
      header { grid-template-columns: auto 1fr; }
      .cta { grid-column: 1 / -1; }
      .hero { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="avatar">LF</div>
      <div class="title">
        <h1>@LINEFUNERAL12</h1>
        <p>@BSEFE_2012 • YouTube İçerik Üreticisi</p>
      </div>
      <div class="cta">
        <a href="https://www.youtube.com/@BSEFE_2012" target="_blank">YouTube'a Git</a>
      </div>
    </header>

    <section class="hero">
      <div>
        <h2>Kafamı dağıtmak için içerik üretiyorum.</h2>
        <p>Bu kanal para kazanmak için değil; eğlenmek, denemek ve paylaşmak için var.
        Modern, sade ve karanlık temalı bir alan: LINEFUNERAL12.</p>
        <div class="stats">
          <div class="stat"><strong>818+</strong>Abone</div>
          <div class="stat"><strong>36</strong>Video</div>
          <div class="stat"><strong>Aktif</strong>Kanal</div>
        </div>
      </div>
      <div class="card">
        <h3>Hakkımda</h3>
        <p>Merhaba, ben Beyazit. Bu site YouTube kanalımla ilgili tüm bilgileri
        tek bir yerde toplamak için hazırlandı. Yeni videolar, duyurular
        ve kanal güncellemeleri burada olacak.</p>
      </div>
    </section>

            <section style="margin-bottom:80px">
      <h2 style="margin-bottom:24px">Videolar</h2>
      <div class="card" style="text-align:center">
        <p style="margin-bottom:20px;color:#d1d5db">Tüm videolarımı ve Shorts içeriklerimi YouTube kanalımda izleyebilirsin.</p>
        <a href="https://www.youtube.com/@BSEFE_2012" target="_blank" style="display:inline-block;background:linear-gradient(135deg,#ef4444,#b91c1c);padding:16px 28px;border-radius:999px;font-weight:700;box-shadow:0 10px 25px rgba(239,68,68,.35)">YouTube Kanalına Git ▶</a>
      </div>
    </section>

            <section style="margin-bottom:80px">
      <h2 style="margin-bottom:24px">Mesaj Bırak</h2>
      <div class="card">
        <form onsubmit="handleMessage(event)">
          <div style="display:grid; gap:16px">
            <input type="text" id="name" placeholder="İsmin" required style="padding:14px 16px;border-radius:14px;border:1px solid rgba(255,255,255,.15);background:#020617;color:#e5e7eb;font-size:15px;">
            <textarea id="message" rows="5" placeholder="Mesajın" required style="padding:14px 16px;border-radius:14px;border:1px solid rgba(255,255,255,.15);background:#020617;color:#e5e7eb;font-size:15px;resize:none;"></textarea>
            <button type="submit" style="align-self:flex-start;background:linear-gradient(135deg,#22c55e,#15803d);border:none;padding:12px 22px;border-radius:999px;font-weight:600;color:#022c22;cursor:pointer;">Gönder</button>
          </div>
        </form>
        <p id="success" style="display:none;margin-top:16px;color:#22c55e;">Mesaj alındı ✔</p>
      </div>

      <div class="card" style="margin-top:32px">
        <h3 style="margin-bottom:16px">Gönderilen Mesajlar</h3>
        <div id="messageList" style="display:grid; gap:14px"></div>
      </div>
    </section>

    <footer>
      © 2026 LINEFUNERAL12 • Modern YouTube Kanal Sitesi
    </footer>

        <script>
      const list = document.getElementById('messageList');
      const success = document.getElementById('success');

      function loadMessages() {
        const messages = JSON.parse(localStorage.getItem('messages') || '[]');
        list.innerHTML = '';
        messages.forEach(m => {
          const div = document.createElement('div');
          div.style.padding = '14px';
          div.style.borderRadius = '14px';
          div.style.background = 'rgba(255,255,255,.04)';
          div.innerHTML = `<strong>${m.name}</strong><br><span style="color:#9ca3af">${m.message}</span>`;
          list.prepend(div);
        });
      }

      function handleMessage(e) {
        e.preventDefault();
        const name = document.getElementById('name').value.trim();
        const message = document.getElementById('message').value.trim();
        if (!name || !message) return;

        // 1️⃣ Sayfada göster + 2️⃣ LocalStorage
        const messages = JSON.parse(localStorage.getItem('messages') || '[]');
        messages.push({ name, message });
        localStorage.setItem('messages', JSON.stringify(messages));
        loadMessages();

      // 🎥 VİDEOLAR – SAYFA YÜKLENDİKTEN SONRA OLUŞTUR
      document.addEventListener('DOMContentLoaded', () => {
        const videoGrid = document.getElementById('videoGrid');
        if (!videoGrid) return;

        const videos = [
          {
            title: 'Shorts Video 1',
            url: 'https://www.youtube.com/shorts/KcpDbA8iBgc'
          },
          {
            title: 'Shorts Video 2',
            url: 'https://www.youtube.com/shorts/uQ1nWE4xHjI'
          },
          {
            title: 'Shorts Video 3',
            url: 'https://www.youtube.com/shorts/HhizGIL81Po'
          }
        ];

        videos.forEach(v => {
          let id = '';
          if (v.url.includes('/shorts/')) {
            id = v.url.split('/shorts/')[1].split('?')[0];
          } else if (v.url.includes('v=')) {
            id = v.url.split('v=')[1].split('&')[0];
          }

          if (!id) return;

          const card = document.createElement('a');
          card.className = 'video';
          card.href = v.url;
          card.target = '_blank';
          card.innerHTML = `
            <div class="thumb" style="background-image:url('https://img.youtube.com/vi/${id}/hqdefault.jpg');background-size:cover;background-position:center"></div>
            <div class="info">
              <h4>${v.title}</h4>
              <span>YouTube'da izle</span>
            </div>`;

          videoGrid.appendChild(card);
        });
      });

        // 3️⃣ Profesyonel sistem (Formspree / Firebase için hazır alan)
        fetch('https://formspree.io/f/xxxxxxx', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ name, message })
        }).catch(() => {});

        success.style.display = 'block';
        e.target.reset();
      }

      loadMessages();
    </script>
  </div>
</body>
</html>

